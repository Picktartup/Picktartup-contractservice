apiVersion: v1
kind: Service
metadata:
  name: spring-application
  labels:
    run: spring-application
spec:
  type: NodePort
  ports:
    - port: 8099
      targetPort: 8099
      protocol: TCP
      name: http
  selector:
    run: spring-application